[project]
name = "kafkasecureindocker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "faststream[kafka]>=0.6.6",
    "ruff>=0.15.0",
]
